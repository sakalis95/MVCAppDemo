@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<ul class="navbar-nav text-warning">
    @if (SignInManager.IsSignedIn(User))
    {
        <a></a>
        <a></a>
        <a></a>
        <a></a>

        <a class="navbar navbar-expand-sm navbar-toggleable-sm" id="register" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">Logout</a>

    }
    else
    {
        <a></a>
        <a></a>
        <a></a>
        <a></a>

        <a class="navbar navbar-expand-sm navbar-toggleable-sm" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
        <a class="navbar navbar-expand-sm navbar-toggleable-sm" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>


    }
</ul>
